import { domainsDeviceMgmtDomain } from './domains-device-mgmt-domain';

describe('domainsDeviceMgmtDomain', () => {
  it('should work', () => {
    expect(domainsDeviceMgmtDomain()).toEqual('domains-device-mgmt-domain');
  });
});
