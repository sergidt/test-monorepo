import { coreDomainsCustomerTenantManagement } from './core-domains-customer-tenant-management';

describe('coreDomainsCustomerTenantManagement', () => {
  it('should work', () => {
    expect(coreDomainsCustomerTenantManagement()).toEqual(
      'core-domains-customer-tenant-management'
    );
  });
});
